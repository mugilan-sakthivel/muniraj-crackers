import type { MetadataRoute } from "next"
import { catalogueCategories, catalogueProducts } from "@/data/catalogue.generated"
export default function sitemap(): MetadataRoute.Sitemap { const base = process.env.NEXT_PUBLIC_SITE_URL ?? "https://muniraj-crackers.vercel.app"; const staticPaths = ["", "/catalogue", "/about", "/safety", "/contact", "/faq", "/terms", "/privacy"]; return [...staticPaths.map((path) => ({ url: `${base}${path}`, lastModified: new Date(), changeFrequency: "weekly" as const, priority: path === "" ? 1 : 0.7 })), ...catalogueCategories.map((category) => ({ url: `${base}/catalogue/${category.slug}`, lastModified: new Date(), changeFrequency: "weekly" as const, priority: 0.8 })), ...catalogueProducts.map((product) => ({ url: `${base}/catalogue/${product.categorySlug}/${product.slug}`, lastModified: new Date(), changeFrequency: "weekly" as const, priority: 0.6 }))] }
